apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebs
spec:
  storageClassName: "" 
  VolumeName: ebs ### pv name which given above 
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
# ---
# apiVersion: v1
# kind: Pod
# metadata:
#   name: ebs-static
#   labels:
#     purpose: "ebs-demo"
# spec:
#   nodeSelector:
#     topology.kubernetes.io/zone: us-east-1a
#   containers:
#   - name: nginx
#     image: nginx
#     volumeMounts:
#     - name: persistent-storage
#       mountPath: /usr/share/nginx/html
#   volumes:
#   - name: persistent-storage
#     persistentVolumeClaim:
#       claimName: ebs-static
# ---
# kind: Service
# apiVersion: v1
# metadata:
#   name: nginx-loadbalance
# spec:
#   type: LoadBalancer
#   selector: # labels will used here for selecting the pod
#     purpose: "ebs-demo"
#   ports:
#   - protocol: TCP
#     port: 80
#     targetPort: 80
#     nodePort: 30007